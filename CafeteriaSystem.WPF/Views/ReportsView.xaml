<UserControl x:Class="CafeteriaSystem.WPF.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:lvcore="clr-namespace:LiveChartsCore.SkiaSharpView;assembly=LiveChartsCore.SkiaSharpView">
    <Grid Margin="28,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Bar Chart -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Ingresos Últimos 7 Días (RD$)" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,16"/>
                <lvc:CartesianChart Grid.Row="1" Series="{Binding BarSeries}"
                                    XAxes="{Binding BarXAxes}"
                                    YAxes="{Binding BarYAxes}"
                                    Background="Transparent"/>
            </Grid>
        </Border>
        <!-- Pie Chart -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Distribución por Categoría" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,16"/>
                <lvc:PieChart Grid.Row="1" Series="{Binding PieSeries}" Background="Transparent"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
